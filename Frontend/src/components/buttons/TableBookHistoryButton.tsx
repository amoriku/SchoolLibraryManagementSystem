export const TableBookHistoryButton = ({ onClick }: { onClick: () => void }) => {

    return (
        <button
            className="button button-rose"
            onClick={onClick}
        >
            История
        </button>
    )
}